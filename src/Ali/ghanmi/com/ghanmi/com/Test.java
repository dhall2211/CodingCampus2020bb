package ghanmi.com;

import java.util.Arrays;

public class Test {

    public static void isUpperCase() {
    }
    }
       /* String s = "ThisIsMyString";
        //  String[] r = s.split("(?<=.)(?=\\p{Lu})");
/*
        // System.out.println(Arrays.toString(r));
        boolean checkUpperCase = false;

        char[] chars = s.toCharArray();
int count =0;
        for (int i = 0; i < chars.length - 1; ++i) {
            if (!isUpperCase(chars.charAt[i])) && isUpperCase(chars.charAt(i+1)) {
                checkUpperCase = true;
                if(checkUpperCase){
                    count++;
                    s.split("[A-Z]");

                }
                System.out.println(s.split("[A-Z]"));//explorer
            }
        }
    }
*/